# Canonical public origin used to build OAuth callback URLs (no trailing slash).
APP_URL=https://fanzoom.ir

# Public URL of the PocketBase instance.
NEXT_PUBLIC_POCKETBASE_URL=https://pocketbase.example.com
